#!/bin/sh

eval $(/usr/bin/gnome-keyring-daemon --start --components=pkcs11,secrets,ssh)
export SSH_AUTH_SOCK

export XDG_CURRENT_DESKTOP=GNOME

_wm_name=$1
shift
exec "${_wm_name}" "$@" >>/tmp/"${_wm_name}"-stdout 2>>/tmp/"${_wm_name}"-stderr
